<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8">
    <title>title</title>
    <link rel="stylesheet" href="stylesheets/bootstrap.css" type="text/css">  
  </head>
  <body id="page-top">
      <% if Helpers.is_logged_in?(session) %>
        <h1>Welcome <%= Helpers.current_user(session).username %></h1>
        <p> UserName: <%= Helpers.current_user(session).username %> </p>
        <p> Balance: <%= Helpers.current_user(session).balance.to_s %> </p>
      <% else %>
        <a href="/">Home </a>
      <% end %>
      
      <button><a href="/logout">Log Out</a></button>

  </body>
</html>